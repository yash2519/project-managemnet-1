import { TaskGraphNode } from "../types/dependency";

export const getTasksForGraph = async (): Promise<{ tasks: TaskGraphNode[], truncated: boolean }> => {
  // Skeleton implementation returning mock data
  return {
    tasks: [],
    truncated: false,
  };
};
